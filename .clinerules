# 项目工程契约

## 1. 架构原则 (Architectural Philosophy)
- **极简主义**：拒绝任何非必要的抽象层，代码必须直观。
- **单机离线**：禁止调用任何外部 API，所有业务逻辑必须通过纯代码或本地静态数据实现。
- **性能优先**：严禁在循环内进行字符串拼接或复杂对象创建。查找操作必须优先使用 O(1) 的 Map 或静态数组索引。

## 2. 编码风格 (Coding Style)
- **文档优先**：核心函数必须包含详细中文注释（包含输入参数说明、返回值、算法逻辑）。
- **防御性代码**：所有外部输入（如用户输入、传感器读取）必须在第一时间进行范围校验和类型判断。
- **严禁过度设计**：不需要设计复杂的工厂模式或中间件，直接调用函数即可。

## 3. 测试与验证 (Testing & Validation)
- **测试驱动**：在编写功能逻辑前，先展示其对应的单元测试思路。
- **边界覆盖**：针对农历/天文类逻辑，必须编写针对极端日期（闰月、跨年、节气交界）的测试用例。

## 4. 交互规则 (Interaction Protocol)
- **拒绝猜测**：如果需求逻辑有歧义，必须立即向我提问，严禁自行假设逻辑补全代码。
- **拒绝自动提交**：严禁执行任何自动 `git pull/commit/push` 指令。只负责输出代码片段，由我审核并手动提交。
- **代码完整性**：每次修改代码时，必须给出完整的 Diff View，不要只输出修改部分。

## 5. 项目约束 (Project Constraints)
- **技术栈**：本项目使用flutter（dart）框架开发
- **库依赖**：可以引入第三方包，但需要提前告知，一般情况下使用标准库。
- **开发环境**：如果本地没有的开发环境只能在docker里面安装和调用，严禁在本地安装任何开发环境，当前docker已安装python环境，你随时可以使用，docker命令为docker run -itd --name py_station -v "/Users/happy/Coding/workplace_py:/workspace" -w /workspace -p 8000:8000 python:3.14.3。

## 6. 信息
- **外部信息**：在plan或者act过程中可以使用网路查询相关的信息，验证准确性。
- **获取方式**：允许使用项目以外的工具和计算机语言去获取项目所需的数据。